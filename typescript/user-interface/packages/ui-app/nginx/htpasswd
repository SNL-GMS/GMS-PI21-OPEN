#
# Temporary file used by Nginx to control access to the interactive analysis UI
#
# Each `<USERNAME>:<HASH>` line specifies a login for a single user.
# The hashes can be generated by running `openssl passwd -apr1`.
#
