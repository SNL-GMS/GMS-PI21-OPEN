import { MenuDivider } from '@blueprintjs/core';
import * as React from 'react';

/**
 * Divider entry for checkbox list, created a line to separation
 *
 * @returns a blueprint divider
 */
// eslint-disable-next-line react/function-component-definition
export const DividerEntry: React.FunctionComponent = () => {
  return <MenuDivider />;
};
